// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/inspire-manager/pkg/db/ent/couponspecialoffer"
	"github.com/NpoolPlatform/inspire-manager/pkg/db/ent/predicate"
	"github.com/google/uuid"
)

// CouponSpecialOfferQuery is the builder for querying CouponSpecialOffer entities.
type CouponSpecialOfferQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.CouponSpecialOffer
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CouponSpecialOfferQuery builder.
func (csoq *CouponSpecialOfferQuery) Where(ps ...predicate.CouponSpecialOffer) *CouponSpecialOfferQuery {
	csoq.predicates = append(csoq.predicates, ps...)
	return csoq
}

// Limit adds a limit step to the query.
func (csoq *CouponSpecialOfferQuery) Limit(limit int) *CouponSpecialOfferQuery {
	csoq.limit = &limit
	return csoq
}

// Offset adds an offset step to the query.
func (csoq *CouponSpecialOfferQuery) Offset(offset int) *CouponSpecialOfferQuery {
	csoq.offset = &offset
	return csoq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (csoq *CouponSpecialOfferQuery) Unique(unique bool) *CouponSpecialOfferQuery {
	csoq.unique = &unique
	return csoq
}

// Order adds an order step to the query.
func (csoq *CouponSpecialOfferQuery) Order(o ...OrderFunc) *CouponSpecialOfferQuery {
	csoq.order = append(csoq.order, o...)
	return csoq
}

// First returns the first CouponSpecialOffer entity from the query.
// Returns a *NotFoundError when no CouponSpecialOffer was found.
func (csoq *CouponSpecialOfferQuery) First(ctx context.Context) (*CouponSpecialOffer, error) {
	nodes, err := csoq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{couponspecialoffer.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (csoq *CouponSpecialOfferQuery) FirstX(ctx context.Context) *CouponSpecialOffer {
	node, err := csoq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CouponSpecialOffer ID from the query.
// Returns a *NotFoundError when no CouponSpecialOffer ID was found.
func (csoq *CouponSpecialOfferQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = csoq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{couponspecialoffer.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (csoq *CouponSpecialOfferQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := csoq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CouponSpecialOffer entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CouponSpecialOffer entity is found.
// Returns a *NotFoundError when no CouponSpecialOffer entities are found.
func (csoq *CouponSpecialOfferQuery) Only(ctx context.Context) (*CouponSpecialOffer, error) {
	nodes, err := csoq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{couponspecialoffer.Label}
	default:
		return nil, &NotSingularError{couponspecialoffer.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (csoq *CouponSpecialOfferQuery) OnlyX(ctx context.Context) *CouponSpecialOffer {
	node, err := csoq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CouponSpecialOffer ID in the query.
// Returns a *NotSingularError when more than one CouponSpecialOffer ID is found.
// Returns a *NotFoundError when no entities are found.
func (csoq *CouponSpecialOfferQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = csoq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{couponspecialoffer.Label}
	default:
		err = &NotSingularError{couponspecialoffer.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (csoq *CouponSpecialOfferQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := csoq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CouponSpecialOffers.
func (csoq *CouponSpecialOfferQuery) All(ctx context.Context) ([]*CouponSpecialOffer, error) {
	if err := csoq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return csoq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (csoq *CouponSpecialOfferQuery) AllX(ctx context.Context) []*CouponSpecialOffer {
	nodes, err := csoq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CouponSpecialOffer IDs.
func (csoq *CouponSpecialOfferQuery) IDs(ctx context.Context) ([]uuid.UUID, error) {
	var ids []uuid.UUID
	if err := csoq.Select(couponspecialoffer.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (csoq *CouponSpecialOfferQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := csoq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (csoq *CouponSpecialOfferQuery) Count(ctx context.Context) (int, error) {
	if err := csoq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return csoq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (csoq *CouponSpecialOfferQuery) CountX(ctx context.Context) int {
	count, err := csoq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (csoq *CouponSpecialOfferQuery) Exist(ctx context.Context) (bool, error) {
	if err := csoq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return csoq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (csoq *CouponSpecialOfferQuery) ExistX(ctx context.Context) bool {
	exist, err := csoq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CouponSpecialOfferQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (csoq *CouponSpecialOfferQuery) Clone() *CouponSpecialOfferQuery {
	if csoq == nil {
		return nil
	}
	return &CouponSpecialOfferQuery{
		config:     csoq.config,
		limit:      csoq.limit,
		offset:     csoq.offset,
		order:      append([]OrderFunc{}, csoq.order...),
		predicates: append([]predicate.CouponSpecialOffer{}, csoq.predicates...),
		// clone intermediate query.
		sql:    csoq.sql.Clone(),
		path:   csoq.path,
		unique: csoq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CouponSpecialOffer.Query().
//		GroupBy(couponspecialoffer.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (csoq *CouponSpecialOfferQuery) GroupBy(field string, fields ...string) *CouponSpecialOfferGroupBy {
	grbuild := &CouponSpecialOfferGroupBy{config: csoq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := csoq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return csoq.sqlQuery(ctx), nil
	}
	grbuild.label = couponspecialoffer.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.CouponSpecialOffer.Query().
//		Select(couponspecialoffer.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (csoq *CouponSpecialOfferQuery) Select(fields ...string) *CouponSpecialOfferSelect {
	csoq.fields = append(csoq.fields, fields...)
	selbuild := &CouponSpecialOfferSelect{CouponSpecialOfferQuery: csoq}
	selbuild.label = couponspecialoffer.Label
	selbuild.flds, selbuild.scan = &csoq.fields, selbuild.Scan
	return selbuild
}

func (csoq *CouponSpecialOfferQuery) prepareQuery(ctx context.Context) error {
	for _, f := range csoq.fields {
		if !couponspecialoffer.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if csoq.path != nil {
		prev, err := csoq.path(ctx)
		if err != nil {
			return err
		}
		csoq.sql = prev
	}
	if couponspecialoffer.Policy == nil {
		return errors.New("ent: uninitialized couponspecialoffer.Policy (forgotten import ent/runtime?)")
	}
	if err := couponspecialoffer.Policy.EvalQuery(ctx, csoq); err != nil {
		return err
	}
	return nil
}

func (csoq *CouponSpecialOfferQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CouponSpecialOffer, error) {
	var (
		nodes = []*CouponSpecialOffer{}
		_spec = csoq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*CouponSpecialOffer).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &CouponSpecialOffer{config: csoq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(csoq.modifiers) > 0 {
		_spec.Modifiers = csoq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, csoq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (csoq *CouponSpecialOfferQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := csoq.querySpec()
	if len(csoq.modifiers) > 0 {
		_spec.Modifiers = csoq.modifiers
	}
	_spec.Node.Columns = csoq.fields
	if len(csoq.fields) > 0 {
		_spec.Unique = csoq.unique != nil && *csoq.unique
	}
	return sqlgraph.CountNodes(ctx, csoq.driver, _spec)
}

func (csoq *CouponSpecialOfferQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := csoq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (csoq *CouponSpecialOfferQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   couponspecialoffer.Table,
			Columns: couponspecialoffer.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: couponspecialoffer.FieldID,
			},
		},
		From:   csoq.sql,
		Unique: true,
	}
	if unique := csoq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := csoq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, couponspecialoffer.FieldID)
		for i := range fields {
			if fields[i] != couponspecialoffer.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := csoq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := csoq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := csoq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := csoq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (csoq *CouponSpecialOfferQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(csoq.driver.Dialect())
	t1 := builder.Table(couponspecialoffer.Table)
	columns := csoq.fields
	if len(columns) == 0 {
		columns = couponspecialoffer.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if csoq.sql != nil {
		selector = csoq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if csoq.unique != nil && *csoq.unique {
		selector.Distinct()
	}
	for _, m := range csoq.modifiers {
		m(selector)
	}
	for _, p := range csoq.predicates {
		p(selector)
	}
	for _, p := range csoq.order {
		p(selector)
	}
	if offset := csoq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := csoq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (csoq *CouponSpecialOfferQuery) ForUpdate(opts ...sql.LockOption) *CouponSpecialOfferQuery {
	if csoq.driver.Dialect() == dialect.Postgres {
		csoq.Unique(false)
	}
	csoq.modifiers = append(csoq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return csoq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (csoq *CouponSpecialOfferQuery) ForShare(opts ...sql.LockOption) *CouponSpecialOfferQuery {
	if csoq.driver.Dialect() == dialect.Postgres {
		csoq.Unique(false)
	}
	csoq.modifiers = append(csoq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return csoq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (csoq *CouponSpecialOfferQuery) Modify(modifiers ...func(s *sql.Selector)) *CouponSpecialOfferSelect {
	csoq.modifiers = append(csoq.modifiers, modifiers...)
	return csoq.Select()
}

// CouponSpecialOfferGroupBy is the group-by builder for CouponSpecialOffer entities.
type CouponSpecialOfferGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (csogb *CouponSpecialOfferGroupBy) Aggregate(fns ...AggregateFunc) *CouponSpecialOfferGroupBy {
	csogb.fns = append(csogb.fns, fns...)
	return csogb
}

// Scan applies the group-by query and scans the result into the given value.
func (csogb *CouponSpecialOfferGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := csogb.path(ctx)
	if err != nil {
		return err
	}
	csogb.sql = query
	return csogb.sqlScan(ctx, v)
}

func (csogb *CouponSpecialOfferGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range csogb.fields {
		if !couponspecialoffer.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := csogb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := csogb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (csogb *CouponSpecialOfferGroupBy) sqlQuery() *sql.Selector {
	selector := csogb.sql.Select()
	aggregation := make([]string, 0, len(csogb.fns))
	for _, fn := range csogb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(csogb.fields)+len(csogb.fns))
		for _, f := range csogb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(csogb.fields...)...)
}

// CouponSpecialOfferSelect is the builder for selecting fields of CouponSpecialOffer entities.
type CouponSpecialOfferSelect struct {
	*CouponSpecialOfferQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (csos *CouponSpecialOfferSelect) Scan(ctx context.Context, v interface{}) error {
	if err := csos.prepareQuery(ctx); err != nil {
		return err
	}
	csos.sql = csos.CouponSpecialOfferQuery.sqlQuery(ctx)
	return csos.sqlScan(ctx, v)
}

func (csos *CouponSpecialOfferSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := csos.sql.Query()
	if err := csos.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (csos *CouponSpecialOfferSelect) Modify(modifiers ...func(s *sql.Selector)) *CouponSpecialOfferSelect {
	csos.modifiers = append(csos.modifiers, modifiers...)
	return csos
}
